import AdminLogin from './components/AdminLogin'
import {RouteComponentProps} from '@reach/router'

export const AdminLoginPage = (props: RouteComponentProps) => {
  return <AdminLogin onLogin={() => {}} onLiveDemo={() => {}} />
}
