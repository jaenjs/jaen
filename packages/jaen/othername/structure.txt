/packages
    /jaen
    /jaen-ui
    /jaen-settings: settings tab
    /jaen-notify: match on path or component type
    /jaen-pages: obtion to opt out of pages tab / uses jaen notify

Standalone:
- Content editing of static pages -> no page tree
- jaenSite settings
- Pagetree
- notify


jaen 
    -> jaen-ui
    -> jaen-settings
        -> snek-finder
    -> jaen-pages
        -> jaen-notify
        -> snek-finder

jaen/
    src/
        core/
            <ui.Dashboard tabs=[pages, settings] />
            ui/
            gatsby/
            snek-finder => one snekfinder context for all subapps
            gatsby-config
                plugins: "../pages", "../settings"
        pages/
            gatsby/
            redux/
                internal.ts
                    - types.ts
                        IJaenStatus {
                            isEditing
                            changes {
                                pageId: {total}
                            }
                        }
                        IJaenState
                            pages: {
                                lastAddedNode
                                nodes: ... Partial<JaenPage>
                            }
                            routing: {
                                dynamicPaths: {}
                            }
            fields/
            hooks/
                internal.ts
                    useJaenPageImage
                    useJaenPageTree
                    useJaenTemplates
                    !!no withSnekFinder => useless
                public.ts
                    connectPage
                    connectSection
                    connectTemplate
            types.ts
                IJaenPage (with all subtypes Only split types if neceserry)
        settings/

fieldsprovider? Required for fields in jaen-notify